#include<bits/stdc++.h>
using namespace std;
int main()
{
    int a,b,count=0;
    cin>>a>>b;
    while(1)
    {
        count++;
        a=3*a;
        b=2*b;
        if(a>b)
        {
            cout<<count;
            break;
        }
    }
    return 0;
}
